{% if errno is defined %}
	{% if errno != '' %}
	    <div class="well text-center">
    	    {% for each in errno %}
            	<h3 style="color:red;"><strong>{{ each }}</strong></h3>
        	{% endfor %}
    	</div>
	{% endif %}
{% endif %}