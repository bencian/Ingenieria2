{% if errno is defined %}
	{% if errno != '' or (errno|length > 0) %} {# CONTROLAR SI LLEGA UN ARREGLO VACIO #}
	    <div class="well text-center">
    	    {% for each in errno %}
            	<h3 style="color:red;"><strong>{{ each }}</strong></h3>
        	{% endfor %}
    	</div>
	{% endif %}
{% endif %}